import 'package:flutter/material.dart';

class FirestNav extends StatelessWidget {
  const FirestNav({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Container(
          color: Colors.red,
        ),
      ),
    );
  }
}
